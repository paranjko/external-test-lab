# OpenAI Responses adapter

The catalog lives at [`skills/openai-responses-adapter/`](../../skills/openai-responses-adapter/).

This folder is a pointer so `/broker-compat/responses-adapter/` stays findable.
