## gonkactl-test release

Test a pinned DevShard release against local feature scenarios

### Synopsis

Verify the official release archive, run supported feature bindings in an owned fixture, and write a per-scenario report. Unbound scenarios remain not_run.

```
gonkactl-test release [flags]
```

### Options

```
  -a, --archive string          optional local release archive for offline use
  -d, --data-root string        report root (default: ./build/report; or DATA_ROOT)
  -f, --features string         feature directory or file (default: ./feature; or FEATURES)
  -h, --help                    help for release
  -s, --release-sha256 string   official archive SHA-256 (or RELEASE_SHA256)
  -t, --release-tag string      release tag (or RELEASE_TAG)
  -S, --source string           optional local checkout at the release commit for offline use
```

### Options inherited from parent commands

```
  -c, --config string   optional configuration file
  -o, --format string   output format: text or json (default "text")
  -v, --verbose         enable verbose diagnostics
```

### SEE ALSO

* [gonkactl-test](gonkactl-test.md)	 - Run and verify Gonka test fixtures

###### Auto generated by spf13/cobra on 14-Sep-2026
