{
  email {$ACME_EMAIL}
  admin 127.0.0.1:2019
}
{$PUBLIC_HOST} {
  encode zstd gzip
  @root path /
  redir @root /dashboard/gonka/gov 308
  # The Network Node proxy accepts its public edge as a trusted upstream.
  # Send participant-local public traffic through the public edge so callback requests
  # do not arrive at the proxy from its own Docker bridge and get rejected.
  reverse_proxy https://{$PUBLIC_EDGE_HOST} {
    transport http {
      tls_server_name {$PUBLIC_EDGE_HOST}
    }
    header_up Host {$PUBLIC_EDGE_HOST}
  }
  header {
    X-Content-Type-Options nosniff
    Referrer-Policy no-referrer
  }
}
